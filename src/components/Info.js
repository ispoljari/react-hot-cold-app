import React from 'react';

const Info = () => {
  const toggleInfo = () => {
    //something
  }

  return (
    <div>
      <button type="button" onClick={toggleInfo()}>
        How To Play
      </button>
    </div>
  );
}

export default Info;